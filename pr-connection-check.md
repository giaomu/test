# Pull request connection check

This document exercises branch creation, a file commit, and a draft pull request.

It contains no application code changes and can be discarded after verification.

This follow-up commit exercises the local Git command-line push path.
